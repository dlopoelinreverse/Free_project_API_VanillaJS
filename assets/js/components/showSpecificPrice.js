const showSpecificPrice = (cardData) => {
  const btnContainer = document.querySelector(".card-buttons-container");
  console.log(btnContainer);
  btnContainer.innerHTML = "<h2>ggg</h2>";
  //   for (const chooseBtn of currencySelectionBtn)
  //     chooseBtn.addEventListener("click", () =>
  //       console.log(cardData, chooseBtn.id)
  //     );
};
export default showSpecificPrice;
